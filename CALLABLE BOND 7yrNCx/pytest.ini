[pytest]
testpaths = tests
filterwarnings =
     error::DeprecationWarning
    # the "maturity == longest curve tenor" warning is deliberate; individual
    # tests assert it with pytest.warns, the rest may ignore it
    ignore:maturity .* equals the curve's longest tenor:UserWarning
